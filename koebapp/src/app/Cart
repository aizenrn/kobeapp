<ion-header [translucent]="true">
  <ion-toolbar color="dark">
    <ion-buttons slot="start">
      <ion-back-button defaultHref="/home"></ion-back-button>
    </ion-buttons>
    <ion-title>Your Cart ({{ cartItems.length }})</ion-title>
    <ion-buttons slot="end">
      <ion-button (click)="goBack()">
        <ion-icon slot="icon-only" name="close-outline"></ion-icon>
      </ion-button>
    </ion-buttons>
  </ion-toolbar>
</ion-header>

<ion-content [fullscreen]="true" class="page-bg ion-padding">
  <div *ngIf="cartItems.length > 0; else emptyCart">
    <ion-list lines="none" class="cart-list">
      <ion-item *ngFor="let item of cartItems; let i = index" class="cart-item-card">
        <div class="item-visual-bg" [style.background-color]="item.bgColor"></div>
        <ion-thumbnail slot="start">
          <img [src]="item.image" [alt]="item.name" />
        </ion-thumbnail>
        <ion-label>
          <p class="item-name">{{ item.name }}</p>
          <p class="item-price">${{ item.price * item.quantity | number:'1.2-2' }}</p>
          <ion-note color="medium">Qty: {{ item.quantity }}</ion-note>
        </ion-label>
        <ion-buttons slot="end">
          <ion-button color="danger" (click)="removeItem(i)">
            <ion-icon slot="icon-only" name="trash-outline"></ion-icon>
          </ion-button>
        </ion-buttons>
      </ion-item>
    </ion-list>

    <ion-card class="checkout-summary">
      <ion-card-header>
        <ion-card-title>Order Summary</ion-card-title>
      </ion-card-header>
      <ion-card-content>
        <p>Subtotal: <strong>${{ total | number:'1.2-2' }}</strong></p>
        <p>Shipping: <strong>$15.00</strong></p>
        <ion-item lines="none" class="total-row">
          <ion-label slot="start">Total:</ion-label>
          <ion-label slot="end" class="total-price">${{ total + 15 | number:'1.2-2' }}</ion-label>
        </ion-item>
      </ion-card-content>
    </ion-card>
    
    <ion-button 
      expand="block" 
      color="dark" 
      class="checkout-btn ion-margin-top" 
      (click)="checkout()">
      Checkout
      <ion-icon slot="end" name="arrow-forward"></ion-icon>
    </ion-button>

  </div>
  
  <ng-template #emptyCart>
    <div class="empty-cart-message ion-text-center">
      <ion-icon name="cart-outline" color="medium"></ion-icon>
      <p>Your cart is empty. Start shopping now!</p>
      <ion-button routerLink="/home" fill="solid" color="dark">Go to Home</ion-button>
    </div>
  </ng-template>
</ion-content>
